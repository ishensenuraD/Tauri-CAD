export interface LShapeShape {
  outerWidth: number;
  outerHeight: number;
  innerWidth: number;
  innerHeight: number;
}

export const defaultLShape: LShapeShape = {
  outerWidth: 100,
  outerHeight: 80,
  innerWidth: 40,
  innerHeight: 40
};
